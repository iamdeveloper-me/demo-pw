import { EventsearchPipe } from './eventsearch.pipe';

describe('EventsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
